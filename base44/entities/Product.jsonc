{
  "name": "Product",
  "type": "object",
  "properties": {
    "name": {
      "type": "string"
    },
    "name_ar": {
      "type": "string",
      "description": "Arabic display name shown when the site language is Arabic."
    },
    "slug": {
      "type": "string"
    },
    "sku": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "description_ar": {
      "type": "string",
      "description": "Arabic long description shown on the product page in Arabic."
    },
    "short_description": {
      "type": "string"
    },
    "short_description_ar": {
      "type": "string",
      "description": "Arabic short description."
    },
    "price": {
      "type": "number"
    },
    "compare_at_price": {
      "type": "number"
    },
    "stock": {
      "type": "number",
      "default": 0
    },
    "stock_status": {
      "type": "string",
      "enum": [
        "in_stock",
        "out_of_stock",
        "preorder"
      ],
      "default": "in_stock"
    },
    "category": {
      "type": "string"
    },
    "brand": {
      "type": "string"
    },
    "images": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "draft",
        "archived",
        "out_of_stock"
      ],
      "default": "active",
      "description": "active=live on storefront; inactive=temporarily hidden (reactivate later); draft=incomplete, never live; archived=removed from catalog, kept for history and can be restored; out_of_stock=active listing but shown as unavailable to customers."
    },
    "completion_percentage": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "maximum": 100,
      "description": "0\u2013100 share of the publish-required fields that are filled. Recomputed and stored on every save."
    },
    "last_edited_at": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the most recent save (draft autosave or publish). Updated on every persist."
    },
    "featured": {
      "type": "boolean",
      "default": false
    },
    "is_new_arrival": {
      "type": "boolean",
      "default": false
    },
    "is_best_seller": {
      "type": "boolean",
      "default": false
    },
    "weight": {
      "type": "number"
    },
    "dimensions": {
      "type": "string"
    },
    "shipping_class": {
      "type": "string"
    },
    "tax_class": {
      "type": "string"
    },
    "meta_title": {
      "type": "string"
    },
    "meta_description": {
      "type": "string"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "rating": {
      "type": "number",
      "default": 0
    },
    "num_reviews": {
      "type": "number",
      "default": 0
    },
    "return_days": {
      "type": "number",
      "default": 0,
      "description": "Allowed return window in days. 0 or empty means no returns allowed."
    },
    "warranty": {
      "type": "string",
      "description": "Warranty terms set by admin, e.g. '2-year manufacturer warranty'."
    },
    "sale_ends_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the current sale ends. Used to show a countdown; clearing it ends the sale."
    },
    "archived_at": {
      "type": "string",
      "format": "date-time",
      "description": "When the product was archived. Cleared on restore."
    },
    "archived_by": {
      "type": "string",
      "description": "Name of the admin who archived the product. Cleared on restore."
    }
  },
  "required": [
    "name",
    "price"
  ],
  "rls": {
    "read": true,
    "create": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "product_manager"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "product_manager"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "product_manager"
          }
        }
      ]
    }
  }
}