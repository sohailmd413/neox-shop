{
  "name": "Order",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "paid",
        "packed",
        "shipped",
        "out_for_delivery",
        "delivered",
        "cancelled",
        "refunded"
      ],
      "default": "pending"
    },
    "items": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "product_id": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "name_ar": {
            "type": "string",
            "description": "Arabic product name snapshot at purchase time, used for bilingual invoices."
          },
          "sku": {
            "type": "string",
            "description": "Product SKU snapshot at purchase time."
          },
          "image": {
            "type": "string"
          },
          "price": {
            "type": "number"
          },
          "quantity": {
            "type": "number"
          }
        }
      }
    },
    "subtotal": {
      "type": "number",
      "default": 0
    },
    "tax": {
      "type": "number",
      "default": 0
    },
    "shipping_fee": {
      "type": "number",
      "default": 0
    },
    "discount": {
      "type": "number",
      "default": 0
    },
    "total": {
      "type": "number",
      "default": 0
    },
    "shipping_address": {
      "type": "object",
      "description": "Snapshot of the shipping address used at checkout time. Copied from the selected Address (or entered form) so historical orders are unaffected by later edits/deletes in the address book.",
      "properties": {
        "name": {
          "type": "string"
        },
        "line1": {
          "type": "string"
        },
        "line2": {
          "type": "string"
        },
        "city": {
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "postal_code": {
          "type": "string"
        },
        "country": {
          "type": "string"
        },
        "phone": {
          "type": "string"
        }
      }
    },
    "billing_address": {
      "type": "object",
      "description": "Billing address. Falls back to the shipping address when not provided.",
      "properties": {
        "name": {
          "type": "string"
        },
        "line1": {
          "type": "string"
        },
        "line2": {
          "type": "string"
        },
        "city": {
          "type": "string"
        },
        "state": {
          "type": "string"
        },
        "postal_code": {
          "type": "string"
        },
        "country": {
          "type": "string"
        },
        "phone": {
          "type": "string"
        }
      }
    },
    "coupon_code": {
      "type": "string"
    },
    "stripe_payment_id": {
      "type": "string"
    },
    "customer_email": {
      "type": "string",
      "description": "Customer email captured at checkout."
    },
    "payment_method": {
      "type": "string",
      "enum": [
        "card",
        "cod",
        "wallet",
        "upi",
        "net_banking"
      ],
      "default": "card"
    },
    "courier": {
      "type": "string",
      "description": "Shipping/courier partner assigned by admin."
    },
    "tracking_number": {
      "type": "string",
      "description": "Shipment tracking number assigned by admin."
    },
    "invoice_number": {
      "type": "string",
      "description": "Sequential invoice number, e.g. INV-2026-000123. Assigned on first invoice generation."
    },
    "invoice_status": {
      "type": "string",
      "enum": [
        "not_generated",
        "generated",
        "sent"
      ],
      "default": "not_generated",
      "description": "Tracks whether an invoice PDF has been generated and/or sent to the customer."
    },
    "invoice_generated_at": {
      "type": "string",
      "format": "date-time"
    },
    "invoice_sent_at": {
      "type": "string",
      "format": "date-time"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Admin tags e.g. priority, gift, b2b."
    },
    "notes": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string"
          },
          "author": {
            "type": "string"
          },
          "created_date": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "description": "Internal admin notes."
    },
    "timeline": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "status": {
            "type": "string"
          },
          "by": {
            "type": "string"
          },
          "at": {
            "type": "string",
            "format": "date-time"
          }
        }
      },
      "description": "Status change history log. Each entry records the new status, who changed it, and when \u2014 the source of truth for the customer-facing order tracker timestamps."
    }
  },
  "required": [
    "status"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "delivery_manager"
          }
        }
      ]
    },
    "create": true,
    "update": {
      "$or": [
        {
          "user_condition": {
            "role": "admin"
          }
        },
        {
          "user_condition": {
            "role": "delivery_manager"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}